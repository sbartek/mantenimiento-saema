<%= form_for(@zona) do |f| %>
  <% if @zona.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@zona.errors.count, "error") %> prohibited this zona from being saved:</h2>

      <ul>
      <% @zona.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.number_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :area_pt %><br>
    <%= f.number_field :area_pt %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
